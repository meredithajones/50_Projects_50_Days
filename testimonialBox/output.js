const firstNum = 60;
const lastNum = 70;
let outputNum = 0;

// Complete the countUp function below.
function countUp(start) {
for( firstNum; firstNum < lastNum; firstNum++ ) {
    outputNum = outputNum + firstNum;
}
    // document.write (outputNum + "then") ;
    console.log(outputNum);
}

console.log(outputNum);